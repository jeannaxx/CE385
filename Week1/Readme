Workshop1.1  
-การประกาศตัวเเปรตามที่โจทย์กำหนดก่อน ด้วยการใข้let เป็นค่าที่เปลี่ยนแปลงได้ตลอด 
เเล้วใช้console.logในการเรียกใช้ตัวแปรต่างๆตามโค้ด เป็นการตั้งใช้stringเเล้วเรียกใช้ ตัวแปรที่เรากำหนด

let fristname = "ณัฐธยาน์ เดชา" ;  //ประกาศตัวแปรตามที่โจทย์สั่ง
let Branch ="วิศวคอมพิวเตอร์" ;
let age = 20 ;

console.log("ชื่อ:",fristname); //การเรียกใช้ให้โชว์ขึ้น
console.log("สาขา:",Branch);
console.log("อายุ:",age + " ปี "); //การเรียกใช้ เเล้วให้เเสดงปี ลงท้าย

Workshop1.2
-การเช๊คไทปต่างๆดวยการใช้typeof ตามค่าตัวแปรที่เรากำหนด ด้วยการใช้let ในการประกาศ เเล้วกำหนดตามที่เราจะต้องการเช๊คไทป์ของตัวนั้้นๆ
เเล้วใช้console.log การเรียกใช้ การที่สร้างstringตามโจทย์ ตามด้วยเรียกตัวแปรที่เราตั้งชื่อไว้ เเล้วเช๊คไทป์

let nickname = "jenny"; //การกำหนดตัวเเปร string
let Number = 22; // Number
let isid = false; //boolean
let emptyValue = null; // Null
let Defined ; //Undefined

console.log("myString:" , nickname ," - Type:" ,typeof  nickname );  //โชว์ขึ้น ด้วยการเพิ่มตstring เเล้วเรียกใช้nickname เเเล้วตรวจสอบไทป์ด้วยtypeof
console.log("myNumber:" , Number ," - Type:" ,typeof  Number );
console.log("myBoolran:" , isid," - Type:" ,typeof  isid);
console.log("myNull:" , emptyValue," - Type:" ,typeof  emptyValue);
console.log("myundefined:" , Defined," - Type:" ,typeof  Defined);

Workshop1.3
-การใช้ตัวการดำเนินการทางคณิตศาสตร์  การบวก การหารเเละมีตัวเปรียบเทียบเข้ามาด้วย เช่นsum เป้นการบวก ทุกจำนวนเข้ากัน  เเล้วสัญญาลักษณ์ / เท่ากับการหาร การหารหาค่าเฉลี่ย
ส่วนตัว >= มากว่าหรือเท่ากัย80 ไหมถ้าใช้ให้ ถุกต้อง เเสดงเป็นค่าboolean
let Math = 85;
let Sci= 72;
let Eng  = 90;
let sum = Math+Sci+Eng  ; // sumคือการบวก จึงกำหนดตัวแปรตัวรวม เพาระว่ากันกรณีเปลี่ยนตัวเลขจะทำให้ค่าเปลี่ยนเเปลงตามไป

console.log("คะเเนนรวม:" ,sum); //เรียกsum มาจะทำกัยผลรวม 
console.log("คะเเนนเฉลี่ย:", sum /3 ); //หาร3เพาระมี3วิชา ได้ค่าเฉลี่ยออกมา
console.log("คะเเนนเฉลี่ย >= 80 ",sum >= 80 ); // เช็คว่า ค่าsumมากกว่าหรือเท่ากับ 80 ไหม ถ้าใช่จะtrue 

Workshop1.4
-การใช้ if else  ในการตรวจสอบเงื่อนไข ตามโจทย์กำหนด 
== จะเท่ากับ เช๊คว่า ตัวชื่อ ตรงกับที่กำหนดไว้ไหม  ถ้าถูกจะตรงตามเงื่อนไขเเละเช๊คอันต่อไป
&& เเละ เช๊คทั้ง ชื่อ เเละรหัสผ่าน ในเงื่อไขเดดียวกัน 
ถ้าถุกต้องตามเงื่อนไขทุกอัน โค้ดจะวิ่งไปตามเงื่อนไข ถ้าไม่ถูกจะวิ่งข้ามไปตามเงื่อนไขอันสุดท้ายทันที 


//Test1
let usrename = "admin";
let password = "1234";
let age = 22 ;

if (usrename == "admin" && password == "1234" && age >= 18 ) { // กำหนดเงื่อนไขตรงนี้ ถ้าตามเงื่อไขจะไปตบรรทัดต่อไป
    console.log("เข้าสู่ระบบสำเร็จ") //จะเเสดงเมื่อตรงจามเงื่อนไข
} else if (age < 18 ){
    console.log ("อายุไม่ถึงเกณฑ์")
}else {
    console.log("ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง") //เงื่อไขไม่ตรงกับที่กำหนดไว้ข้างบนบรรทัดif
}
